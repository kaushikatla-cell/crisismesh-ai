# CrisisMesh Chat (Mobile)

This is a minimal React Native chat client for the CrisisMesh MVP.

## Setup

1. Initialize a React Native or Expo project.
2. Install dependency:

```bash
npm install react-native-tcp-socket
# or
yarn add react-native-tcp-socket
